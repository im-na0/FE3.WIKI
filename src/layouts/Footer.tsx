import React from "react";
import { Layout } from "antd";

const { Footer } = Layout;

const MainFooter = () => {
  return (
    <Footer>
      <div>&copy; wiki</div>
    </Footer>
  );
};

export default MainFooter;
